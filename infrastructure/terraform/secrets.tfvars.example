# Template for environment-specific secrets — copy to <env>.secrets.tfvars and populate.
# ⚠️  Files matching *.secrets.tfvars are gitignored — never commit them.

youtube_api_key = "<your-youtube-data-api-v3-key>"
